import QtQuick 2.0

Rectangle {
    color: "transparent"
    anchors.fill: parent
    HelpToolGroup {
        id:helpTools
        width: 140
        height: parent.height
        anchors.left: parent.left
        anchors.top: parent.top
    }	
}
